# Files Docker should NOT copy into the build context. Keeps the build fast
# and the image small — and avoids leaking local-only state into the image.

node_modules
.git
.gitignore
.DS_Store
*.log
.env
.env.*

# Markdown docs that aren't needed at runtime
README.md
docs
